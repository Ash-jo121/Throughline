LLM_API_KEY="sk-..."
THROUGHLINE_PUBLIC_BASE_URL="http://localhost:5173"

# Optional: real Jira Cloud import. Use an Atlassian API token, not your password.
# Example site URL: https://your-domain.atlassian.net
JIRA_SITE_URL=""
JIRA_EMAIL=""
JIRA_API_TOKEN=""

# Optional: real Slack share via incoming webhook.
SLACK_WEBHOOK_URL=""

# Optional: override where Cognee writes local embedded state.
# Throughline defaults these under ./.cognee when unset.
# SYSTEM_ROOT_DIRECTORY="./.cognee/system"
# DATA_ROOT_DIRECTORY="./.cognee/data"
# CACHE_ROOT_DIRECTORY="./.cognee/cache"
